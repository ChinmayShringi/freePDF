# Copy to .env.local and fill in before deploying. All values are optional.

# Cloudflare Web Analytics token (cookieless). The beacon only loads when set.
VITE_CF_ANALYTICS_TOKEN=

# Ko-fi username for the optional tip jar (https://ko-fi.com/<handle>).
VITE_KOFI_HANDLE=
